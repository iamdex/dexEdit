import SwiftUI

enum Brand {
    /// The indigo the app icon is built from. Used for anything that should
    /// read as the app itself rather than as a system control.
    static let tint = Color(red: 0.31, green: 0.275, blue: 0.898)
}
